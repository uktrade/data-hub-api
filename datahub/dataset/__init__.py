"""Views for importing data into Data Workspace.

Each view represents a data set. The data flow component of Data Workspace traverses
the pages of each view on a regular basis (typically daily).
"""
