Pipeline item's `name` field is now a required in `POST /v4/pipeline-item` API endpoint.

Pipeline item's `name` field can't be updated to null or empty string with `PATCH /v4/pipeline-item/uuid` API endpoint.
